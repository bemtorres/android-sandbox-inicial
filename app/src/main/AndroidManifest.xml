<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/common_app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sandbox">
        <!-- Flujo: Login (launcher) -> Registro -> Intro -> Home (hub sandbox) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" android:exported="false" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".IntroActivity" android:exported="false" />
        <activity android:name=".HomeActivity" android:exported="false" />

        <!-- Módulo 1: Conoce Android (Labs 1 al 10) -->
        <activity android:name=".sandbox.TextInputsActivity" android:exported="false" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".sandbox.DateTimeActivity" android:exported="false" />
        <activity android:name=".sandbox.ButtonsActivity" android:exported="false" />
        <activity android:name=".sandbox.SelectionActivity" android:exported="false" />
        <activity android:name=".sandbox.FeedbackActivity" android:exported="false" />
        <activity android:name=".sandbox.WebViewActivity" android:exported="false" />
        <activity android:name=".sandbox.DialogToastActivity" android:exported="false" />
        <activity android:name=".sandbox.LayoutsActivity" android:exported="false" />
        <activity android:name=".sandbox.SimpleScrollListActivity" android:exported="false" />
        <activity android:name=".sandbox.ContactRecyclerActivity" android:exported="false" />

        <!-- Módulo 2: Funcionalidades Especiales (Labs 11, 12 y 13) -->
        <activity android:name=".sandbox.ConcurrencyActivity" android:exported="false" />
        <activity android:name=".sandbox.SensorsActivity" android:exported="false" />
        <activity android:name=".sandbox.SpeechActivity" android:exported="false" />
    </application>

</manifest>
